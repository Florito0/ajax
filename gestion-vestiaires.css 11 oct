/* === Gestion Vestiaires === */

.gestion-vestiaires {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

.vestiaires-container {
    background-color: #1e1e1e;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
}

.vestiaires-header {
    background-color: #2c2c2c;
    padding: 20px;
    text-align: center;
    border-bottom: 1px solid #333;
}

.vestiaires-header h1 {
    font-size: clamp(20px, 4vw, 28px);
    font-weight: 700;
    margin-bottom: 10px;
    color: #fff;
}

.vestiaires-header .subtitle {
    font-size: clamp(14px, 3vw, 18px);
    color: #b3b3b3;
}

/* Interface de gestion (admin) */
.vestiaires-admin {
    padding: 20px;
    position: relative;
}

.articles-list {
    margin-bottom: 20px;
}

.article-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px;
    border-bottom: 1px solid #333;
    cursor: move;
    transition: background-color 0.2s;
}

.article-item:hover {
    background-color: #2c2c2c;
}

.article-item.dragging {
    opacity: 0.5;
    background-color: #333;
}

.article-info {
    display: flex;
    align-items: center;
    gap: 15px;
    flex: 1;
}

.article-name {
    font-size: clamp(16px, 3.5vw, 20px);
    font-weight: 500;
    color: #fff;
    flex: 1;
}

.article-price {
    font-size: clamp(16px, 3.5vw, 20px);
    color: #b3b3b3;
    min-width: 80px;
}

.article-actions {
    display: flex;
    gap: 10px;
}

.edit-btn, .delete-btn {
    width: clamp(30px, 8vw, 20px);
    height: clamp(30px, 8vw, 20px);
    border-radius: 50%;
    border: none;
    color: white;
    font-size: clamp(14px, 4vw, 14px);
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
    transition: background-color 0.2s, transform 0.2s;
}

.edit-btn {
    background-color: #9733EE;
}

.edit-btn:hover {
    background-color: #DA22FF;
    transform: scale(1.1);
}

.delete-btn {
    background-color: #ff4444;
}

.delete-btn:hover {
    background-color: #ff6666;
    transform: scale(1.1);
}

.delete-btn:active, .edit-btn:active {
    transform: scale(0.95);
}

/* Formulaire d'ajout/modification */
.article-form {
    padding: 20px;
    background-color: #2c2c2c;
    border-radius: 8px;
    margin-top: 20px;
}

.article-form h3 {
    color: #fff;
    margin-bottom: 15px;
    font-size: clamp(18px, 4vw, 22px);
}

.form-row {
    display: grid;
    grid-template-columns: 2fr 1fr auto;
    gap: 10px;
    align-items: end;
}

.form-group {
    display: flex;
    flex-direction: column;
}

.form-group label {
    color: #b3b3b3;
    margin-bottom: 5px;
    font-size: clamp(14px, 3vw, 16px);
}

.form-group input {
    padding: clamp(8px, 2vw, 12px);
    background-color: #333;
    border: 1px solid #444;
    color: white;
    border-radius: 6px;
    font-size: clamp(14px, 3vw, 16px);
    transition: border-color 0.2s;
}

.form-group input:focus {
    outline: none;
    border-color: #9733EE;
}

.form-group input::placeholder {
    color: #666;
}

.add-article-btn, .update-article-btn, .cancel-edit-btn {
    padding: clamp(10px, 2.5vw, 14px) clamp(15px, 4vw, 20px);
    border: none;
    border-radius: 6px;
    font-size: clamp(14px, 3vw, 16px);
    font-weight: 600;
    cursor: pointer;
    transition: opacity 0.2s, transform 0.2s;
    white-space: nowrap;
}

.add-article-btn, .update-article-btn {
    background: linear-gradient(to right, #9733EE, #DA22FF);
    color: white;
}

.add-article-btn:hover, .update-article-btn:hover {
    opacity: 0.9;
    transform: translateY(-2px);
}

.add-article-btn:active, .update-article-btn:active {
    opacity: 0.8;
    transform: translateY(0);
}

.cancel-edit-btn {
    background: #444;
    color: #b3b3b3;
}

.cancel-edit-btn:hover {
    background: #555;
}

/* Message vide */
.empty-message {
    text-align: center;
    color: #b3b3b3;
    font-size: clamp(16px, 3.5vw, 20px);
    padding: 40px 20px;
}

/* Notification */
.vestiaires-notification {
    position: fixed;
    top: 20px;
    right: 20px;
    background-color: #333;
    color: white;
    padding: 15px 25px;
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
    font-size: clamp(14px, 3vw, 16px);
    z-index: 2000;
    opacity: 0;
    transform: translateX(100%);
    transition: opacity 0.3s ease, transform 0.3s ease;
}

.vestiaires-notification.show {
    opacity: 1;
    transform: translateX(0);
}

.vestiaires-notification.success {
    background: linear-gradient(135deg, #4CAF50, #45a049);
}

.vestiaires-notification.error {
    background: linear-gradient(135deg, #f44336, #da190b);
}

/* Interface Kiosque */
.vestiaires-kiosque {
    padding: 20px;
    position: relative;
}

.kiosque-item {
    display: grid;
    grid-template-columns: 2fr 1fr 2fr;
    align-items: center;
    padding: 15px 0;
    border-bottom: 1px solid #333;
}

.kiosque-item:last-child {
    border-bottom: none;
}

.kiosque-item-name {
    font-size: clamp(16px, 3.5vw, 20px);
    font-weight: 500;
    color: #fff;
}

.kiosque-item-price {
    font-size: clamp(16px, 3.5vw, 20px);
    color: #b3b3b3;
    text-align: center;
}

.kiosque-quantity-control {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 8px;
}

.kiosque-qty-btn {
    width: clamp(32px, 8vw, 40px);
    height: clamp(32px, 8vw, 40px);
    border-radius: 50%;
    border: none;
    background-color: #333;
    color: white;
    font-size: clamp(16px, 4vw, 20px);
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
    transition: background-color 0.2s, transform 0.2s;
}

.kiosque-qty-btn:hover {
    background-color: #444;
    transform: scale(1.05);
}

.kiosque-qty-btn:active {
    background-color: #555;
    transform: scale(0.95);
}

.kiosque-qty-value {
    width: clamp(40px, 10vw, 50px);
    text-align: center;
    font-size: clamp(16px, 3.5vw, 20px);
    font-weight: 600;
    color: #fff;
}

/* Boutons d'action du kiosque */
.vestiaires-actions {
    padding: 20px;
    background-color: #2c2c2c;
    border-top: 1px solid #333;
}

.vestiaires-total {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 15px;
    font-size: clamp(18px, 4vw, 24px);
    font-weight: 700;
    color: #fff;
}

.vestiaires-buttons {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.validate-vestiaires-btn, .skip-vestiaires-btn {
    width: 100%;
    padding: clamp(12px, 3vw, 16px);
    border: none;
    border-radius: 6px;
    font-size: clamp(16px, 3.5vw, 20px);
    font-weight: 600;
    cursor: pointer;
    transition: opacity 0.2s, transform 0.2s;
}

.validate-vestiaires-btn {
    background: linear-gradient(to right, #9733EE, #DA22FF);
    color: white;
}

.validate-vestiaires-btn:hover {
    opacity: 0.9;
    transform: translateY(-2px);
}

.validate-vestiaires-btn:disabled {
    background: #444;
    opacity: 0.6;
    cursor: not-allowed;
    transform: none;
}

.skip-vestiaires-btn {
    background: #444;
    color: #b3b3b3;
    border: 1px solid #666;
}

.skip-vestiaires-btn:hover {
    background: #555;
    color: #ccc;
}

/* Loading spinner */
.vestiaires-loading {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 40px;
}

.spinner {
    width: 40px;
    height: 40px;
    border: 4px solid #333;
    border-top-color: #9733EE;
    border-radius: 50%;
    animation: spin 1s linear infinite;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}

/* Responsive */
@media (max-width: 768px) {
    .form-row {
        grid-template-columns: 1fr;
    }
    
    .cancel-edit-btn {
        margin-left: 0;
        margin-top: 10px;
    }
    
    .kiosque-item {
        grid-template-columns: 1fr 1fr 1fr;
        padding: 10px 0;
    }
}

@media (max-width: 480px) {
    .article-info {
        flex-direction: column;
        align-items: flex-start;
        gap: 5px;
    }
    
    .article-price {
        text-align: left;
    }
    
    .vestiaires-notification {
        right: 10px;
        left: 10px;
    }
}

/* Position relative sur le conteneur parent */
#gestion-tickets-vestiaire {
    position: relative;
}

/* Bouton de bascule Admin/Kiosque */
#toggle-vestiaire-interface-btn {
    background: #333333 !important;
    color: #ffffff !important;
    font-size: 14px !important;
    font-weight: 500 !important;
    padding: 10px 20px !important;
    cursor: pointer !important;
    transition: background-color 0.2s, color 0.2s !important;
    border: none !important;
    border-radius: 8px !important;
    position: absolute !important;
    top: 20px !important;
    right: 20px !important;
    z-index: 100 !important;
    margin-bottom: 0 !important;
}

#toggle-vestiaire-interface-btn:hover {
    background: #444444 !important;
    color: #ffffff !important;
}

#toggle-vestiaire-interface-btn:active {
    transform: translateY(0) !important;
}
