/* === Gestion Vestiaires === */

.gestion-vestiaires {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px;
}

/* Container ADMIN - style classique */
#vestiaires-admin-view.vestiaires-container {
    background-color: #1e1e1e;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
}

/* Container KIOSQUE - style avec effets visuels */
#vestiaires-kiosk-view.vestiaires-container {
    background-color: transparent;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    position: relative;
    z-index: 1;
    border: 1px solid rgba(255, 255, 255, 0.1);
    width: 100%;
    max-width: 600px;
    margin: 60px auto 20px auto;
    padding: 20px;
    box-sizing: border-box;
}

/* Fond dégradé pour le container KIOSQUE */
#vestiaires-kiosk-view.vestiaires-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to right, rgba(255, 255, 255, 0.03), rgba(255, 255, 255, 0.06), rgba(255, 255, 255, 0.03)), #1e1e1e;
    opacity: 1;
    z-index: -1;
}

/* Ligne lumineuse en haut du container KIOSQUE */
#vestiaires-kiosk-view.vestiaires-container::after {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 100%;
    height: 6px;
    background: linear-gradient(
        90deg,
        transparent 0%,
        rgba(255, 255, 255, 0) 5%,
        rgba(255, 255, 255, 0.2) 15%,
        rgba(255, 255, 255, 0.4) 25%,
        rgba(255, 255, 255, 0.7) 40%,
        rgba(255, 255, 255, 1) 50%,
        rgba(255, 255, 255, 0.7) 60%,
        rgba(255, 255, 255, 0.4) 75%,
        rgba(255, 255, 255, 0.2) 85%,
        rgba(255, 255, 255, 0) 95%,
        transparent 100%
    );
    animation: luxuryPulse 4s ease-in-out infinite;
    filter: blur(3px);
    z-index: -1;
}

/* Animation pour la ligne lumineuse */
@keyframes luxuryPulse {
    0%, 100% {
        opacity: 0.4;
    }
    50% {
        opacity: 1;
    }
}

.vestiaires-header {
    background-color: #2c2c2c;
    padding: 20px;
    text-align: center;
    border-bottom: 1px solid #333;
}

/* Header transparent pour le kiosque */
#vestiaires-kiosk-view .vestiaires-header {
    background-color: transparent;
    border-bottom: 1px solid #333;
}

.vestiaires-header h1 {
    font-size: clamp(20px, 4vw, 28px);
    font-weight: 700;
    margin-bottom: 10px;
    color: #fff;
}

.vestiaires-header .subtitle {
    font-size: clamp(14px, 3vw, 18px);
    color: #b3b3b3;
}

/* Header du kiosque vestiaire - marges identiques au kiosque entrée */
#vestiaires-kiosk-view .vestiaires-header h1 {
    padding: 20px;
    text-align: center;
    font-size: 24px;
    font-weight: 700;
    margin: -20px -20px 0px -20px;
}

#vestiaires-kiosk-view .vestiaires-header .subtitle {
    font-size: 16px;
    margin-bottom: 55px;
    color: #b3b3b3;
}

/* Interface de gestion (admin) */
.vestiaires-admin {
    padding: 20px;
    position: relative;
}

.articles-list {
    margin-bottom: 20px;
}

.article-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px;
    border-bottom: 1px solid #333;
    cursor: move;
    transition: background-color 0.2s;
}

.article-item:hover {
    background-color: #2c2c2c;
}

.article-item.dragging {
    opacity: 0.5;
    background-color: #333;
}

.article-info {
    display: flex;
    align-items: center;
    gap: 15px;
    flex: 1;
}

.article-name {
    font-size: clamp(16px, 3.5vw, 20px);
    font-weight: 500;
    color: #fff;
    flex: 1;
}

.article-price {
    font-size: clamp(16px, 3.5vw, 20px);
    color: #b3b3b3;
    min-width: 80px;
}

.article-actions {
    display: flex;
    gap: 10px;
}

.edit-btn, .delete-btn {
    width: clamp(30px, 8vw, 20px);
    height: clamp(30px, 8vw, 20px);
    border-radius: 50%;
    border: none;
    color: white;
    font-size: clamp(14px, 4vw, 14px);
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
    transition: background-color 0.2s, transform 0.2s;
}

.edit-btn {
    background-color: #9733EE;
}

.edit-btn:hover {
    background-color: #DA22FF;
    transform: scale(1.1);
}

.delete-btn {
    background-color: #ff4444;
}

.delete-btn:hover {
    background-color: #ff6666;
    transform: scale(1.1);
}

.delete-btn:active, .edit-btn:active {
    transform: scale(0.95);
}

/* Formulaire d'ajout/modification */
.article-form {
    padding: 20px;
    background-color: #2c2c2c;
    border-radius: 8px;
    margin-top: 20px;
}

.article-form h3 {
    color: #fff;
    margin-bottom: 15px;
    font-size: clamp(18px, 4vw, 22px);
}

.form-row {
    display: grid;
    grid-template-columns: 2fr 1fr auto;
    gap: 10px;
    align-items: end;
}

.form-group {
    display: flex;
    flex-direction: column;
}

.form-group label {
    color: #b3b3b3;
    margin-bottom: 5px;
    font-size: clamp(14px, 3vw, 16px);
}

.form-group input {
    padding: clamp(8px, 2vw, 12px);
    background-color: #333;
    border: 1px solid #444;
    color: white;
    border-radius: 6px;
    font-size: clamp(14px, 3vw, 16px);
    transition: border-color 0.2s;
}

.form-group input:focus {
    outline: none;
    border-color: #9733EE;
}

.form-group input::placeholder {
    color: #666;
}

.add-article-btn, .update-article-btn, .cancel-edit-btn {
    padding: clamp(10px, 2.5vw, 14px) clamp(15px, 4vw, 20px);
    border: none;
    border-radius: 6px;
    font-size: clamp(14px, 3vw, 16px);
    font-weight: 600;
    cursor: pointer;
    transition: opacity 0.2s, transform 0.2s;
    white-space: nowrap;
}

.add-article-btn, .update-article-btn {
    background: linear-gradient(to right, #9733EE, #DA22FF);
    color: white;
}

.add-article-btn:hover, .update-article-btn:hover {
    opacity: 0.9;
    transform: translateY(-2px);
}

.add-article-btn:active, .update-article-btn:active {
    opacity: 0.8;
    transform: translateY(0);
}

.cancel-edit-btn {
    background: #444;
    color: #b3b3b3;
}

.cancel-edit-btn:hover {
    background: #555;
}

/* Message vide */
.empty-message {
    text-align: center;
    color: #b3b3b3;
    font-size: clamp(16px, 3.5vw, 20px);
    padding: 40px 20px;
}

/* Notification */
.vestiaires-notification {
    position: fixed;
    top: 20px;
    right: 20px;
    background-color: #333;
    color: white;
    padding: 15px 25px;
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
    font-size: clamp(14px, 3vw, 16px);
    z-index: 2000;
    opacity: 0;
    transform: translateX(100%);
    transition: opacity 0.3s ease, transform 0.3s ease;
}

.vestiaires-notification.show {
    opacity: 1;
    transform: translateX(0);
}

.vestiaires-notification.success {
    background: linear-gradient(135deg, #4CAF50, #45a049);
}

.vestiaires-notification.error {
    background: linear-gradient(135deg, #f44336, #da190b);
}

/* Interface Kiosque */
.vestiaires-kiosque {
    padding: 20px;
    position: relative;
}

/* Articles du kiosque vestiaire - alignement identique au kiosque entrée */
#vestiaires-kiosk-view .vestiaires-kiosque {
    padding: 20px 0;
}

.kiosque-item {
    display: grid;
    grid-template-columns: 2fr 1fr 2fr;
    align-items: center;
    padding: 15px 0;
    border-bottom: 1px solid #333;
}

#vestiaires-kiosk-view .kiosque-item {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    gap: 15px;
    padding: 15px 0;
    border-bottom: 1px solid #333;
    width: 100%;
    max-width: 500px;
    margin: 0 auto;
}

.kiosque-item:last-child {
    border-bottom: none;
}

.kiosque-item-name {
    font-size: clamp(16px, 3.5vw, 20px);
    font-weight: 500;
    color: #fff;
}

#vestiaires-kiosk-view .kiosque-item-name {
    font-size: 18px;
    font-weight: 500;
    text-align: left;
    line-height: 1.4;
    color: #ffffff;
    word-wrap: break-word;
    word-break: break-word;
    hyphens: auto;
    max-width: 250px;
    min-width: 150px;
}

.kiosque-item-price {
    font-size: clamp(16px, 3.5vw, 20px);
    color: #b3b3b3;
    text-align: center;
}

#vestiaires-kiosk-view .kiosque-item-price {
    font-size: 16px;
    color: #b3b3b3;
    text-align: center;
}

.kiosque-quantity-control {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 8px;
}

/* Ajustement des contrôles de quantité pour le kiosque */
#vestiaires-kiosk-view .kiosque-quantity-control {
    display: flex;
    align-items: center;
    gap: 5px;
    justify-content: flex-end;
}

.kiosque-qty-btn {
    width: clamp(32px, 8vw, 40px);
    height: clamp(32px, 8vw, 40px);
    border-radius: 50%;
    border: none;
    background-color: #333;
    color: white;
    font-size: clamp(16px, 4vw, 20px);
    cursor: pointer;
    display: flex;
    justify-content: center;
    align-items: center;
    transition: background-color 0.2s, transform 0.2s;
}

#vestiaires-kiosk-view .kiosque-qty-btn {
    width: 36px;
    height: 36px;
    font-size: 18px;
}

.kiosque-qty-btn:hover {
    background-color: #444;
    transform: scale(1.05);
}

.kiosque-qty-btn:active {
    background-color: #555;
    transform: scale(0.95);
}

.kiosque-qty-value {
    width: clamp(40px, 10vw, 50px);
    text-align: center;
    font-size: clamp(16px, 3.5vw, 20px);
    font-weight: 600;
    color: #fff;
}

#vestiaires-kiosk-view .kiosque-qty-value {
    width: 40px;
    text-align: center;
    font-size: 18px;
    color: #ffffff;
}

/* Boutons d'action du kiosque */
.vestiaires-actions {
    padding: 20px;
    background-color: #2c2c2c;
    border-top: 1px solid #333;
}

/* Section total du kiosque - marges identiques au kiosque entrée */
#vestiaires-kiosk-view .vestiaires-actions {
    padding: 20px;
    background-color: #2c2c2c;
    border-top: 1px solid #333;
    margin: 20px -20px -20px -20px;
}

.vestiaires-total {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 15px;
    font-size: clamp(18px, 4vw, 24px);
    font-weight: 700;
    color: #fff;
}

#vestiaires-kiosk-view .vestiaires-total {
    display: flex;
    justify-content: space-between;
    font-size: 20px;
    font-weight: 700;
    margin-bottom: 20px;
    color: #ffffff;
}

.vestiaires-buttons {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

/* Boutons d'action du kiosque - États par défaut */
.validate-vestiaires-btn, .skip-vestiaires-btn {
    width: 100%;
    padding: clamp(12px, 3vw, 16px);
    border: none;
    border-radius: 6px;
    font-size: clamp(16px, 3.5vw, 20px);
    font-weight: 600;
    cursor: pointer;
    transition: opacity 0.2s, transform 0.2s, background 0.2s;
}

#vestiaires-kiosk-view .validate-vestiaires-btn,
#vestiaires-kiosk-view .skip-vestiaires-btn {
    padding: 15px;
    font-size: 18px;
}

/* VALIDER LE VESTIAIRE - État actif (quand articles sélectionnés) */
.validate-vestiaires-btn.active {
    background: linear-gradient(to right, #9733EE, #DA22FF);
    color: white;
}

.validate-vestiaires-btn.active:hover {
    opacity: 0.9;
    transform: translateY(-2px);
}

/* VALIDER LE VESTIAIRE - État désactivé (par défaut) */
.validate-vestiaires-btn:not(.active) {
    background: #444;
    color: #666;
    cursor: not-allowed;
    opacity: 0.6;
}

/* CONTINUER SANS VESTIAIRE - État actif (par défaut, aucun article) */
.skip-vestiaires-btn.active {
    background: linear-gradient(to right, #9733EE, #DA22FF);
    color: white;
    border: none;
}

.skip-vestiaires-btn.active:hover {
    opacity: 0.9;
    transform: translateY(-2px);
}

/* CONTINUER SANS VESTIAIRE - État désactivé (quand articles sélectionnés) */
.skip-vestiaires-btn:not(.active) {
    background: #444;
    color: #666;
    border: 1px solid #555;
    cursor: not-allowed;
    opacity: 0.6;
}

/* Loading spinner */
.vestiaires-loading {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 40px;
}

.spinner {
    width: 40px;
    height: 40px;
    border: 4px solid #333;
    border-top-color: #9733EE;
    border-radius: 50%;
    animation: spin 1s linear infinite;
}

@keyframes spin {
    to { transform: rotate(360deg); }
}

/* Responsive */
@media (max-width: 768px) {
    .form-row {
        grid-template-columns: 1fr;
    }
    
    .cancel-edit-btn {
        margin-left: 0;
        margin-top: 10px;
    }
    
    .kiosque-item {
        grid-template-columns: 1fr 1fr 1fr;
        padding: 10px 0;
    }
    
    #vestiaires-kiosk-view .kiosque-item {
        grid-template-columns: 1fr;
        justify-items: center;
        text-align: center;
        gap: 10px;
    }
    
    #vestiaires-kiosk-view .kiosque-item-name {
        text-align: center;
        min-width: 0;
        max-width: 100%;
    }
    
    #vestiaires-kiosk-view .kiosque-quantity-control {
        justify-content: center;
    }
    
    #vestiaires-kiosk-view .kiosque-item-price {
        margin: 10px 0;
    }
}

@media (max-width: 480px) {
    .article-info {
        flex-direction: column;
        align-items: flex-start;
        gap: 5px;
    }
    
    .article-price {
        text-align: left;
    }
    
    .vestiaires-notification {
        right: 10px;
        left: 10px;
    }
}

/* Position relative sur le conteneur parent */
#gestion-tickets-vestiaire {
    position: relative;
    padding-top: 40px;
}

/* Bouton de bascule Admin/Kiosque */
#toggle-vestiaire-interface-btn {
    background: #333333 !important;
    color: #ffffff !important;
    font-size: 14px !important;
    font-weight: 500 !important;
    padding: 10px 20px !important;
    cursor: pointer !important;
    transition: background-color 0.2s, color 0.2s !important;
    border: none !important;
    border-radius: 8px !important;
    position: absolute !important;
    top: 20px !important;
    right: 20px !important;
    z-index: 100 !important;
    margin-bottom: 0 !important;
}

#toggle-vestiaire-interface-btn:hover {
    background: #444444 !important;
    color: #ffffff !important;
}

#toggle-vestiaire-interface-btn:active {
    transform: translateY(0) !important;
}
